<div class="bg-primary text-white px-3 py-2 rounded d-flex flex-wrap gap-4 mb-4">
  <% fields.each do |field| %>
    <div>
      <small class="text-white-50 d-block"><%= field.label %></small>
      <%= project.display_value_for(field) %>
    </div>
  <% end %>
</div>
