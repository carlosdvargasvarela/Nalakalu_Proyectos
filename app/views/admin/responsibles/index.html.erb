<%= panel_card("Responsables") do %>
  <p class="text-muted">
    Personas o equipos que pueden asignarse como responsables de
    proyectos o de etapas puntuales. Un responsable puede vincularse a un
    usuario de la app (para que vea solo lo que le corresponde) o ser
    solo un nombre de referencia.
  </p>
  <%= link_to new_admin_responsible_path, class: "btn btn-primary mb-3" do %>
    <i class="bi bi-plus-lg"></i> Nuevo responsable
  <% end %>
  <ul class="list-group">
    <% @responsibles.each do |responsible| %>
      <li class="list-group-item d-flex justify-content-between align-items-center">
        <span>
          <span class="badge me-2" style="background-color: <%= responsible.color %>">&nbsp;</span><%= responsible.name %>
          <% if responsible.user %><span class="text-muted small">— <%= responsible.user.email %></span><% end %>
        </span>
        <span>
          <%= link_to "Editar", edit_admin_responsible_path(responsible), class: "btn btn-outline-secondary btn-sm" %>
          <%= button_to "Borrar", admin_responsible_path(responsible), method: :delete,
                class: "btn btn-outline-danger btn-sm", form: { style: "display:inline-block", onsubmit: "return confirm('¿Eliminar responsable?')" } %>
        </span>
      </li>
    <% end %>
  </ul>
<% end %>
